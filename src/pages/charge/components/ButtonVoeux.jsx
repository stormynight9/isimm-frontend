import React from "react"
import { Button } from "@/components/ui/Button"
const ButtonVoeux = (props) => {
    return (
        <Button {...props} variant="default">Voeux</Button>
    )
}

export default ButtonVoeux